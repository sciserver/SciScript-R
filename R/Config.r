# make sure none of the URLs have a trailing slash
Config.CasJobsRESTUri = "http://skyserver.sdss.org/CasJobs/RestApi"
Config.AuthenticationURL = "http://portal.sciserver.org/login-portal/keystone/v3/tokens"
Config.SciDriveHost = 'http://www.scidrive.org'
Config.SkyQueryUrl = 'http://voservices.net/skyquery/Api/V1'
Config.DataRelease = 'DR13'
Config.SkyServerWSurl = 'http://skyserver.sdss.org'
Config.ExecuteMode = "release" # if executeMode = "debug", then strings (with useful information) will be printed on screen during the execution, wherever it is specified in the code. If executeMode = "release", then no string will be printed.
